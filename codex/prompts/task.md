Run this ask through the FULL pipeline (Codex track — see the Agentic System section of AGENTS.md) — no trivial-task skip: plan → develop → test → independent code review → functional review → green marker → push. Off-hands rules apply.

The ask: $ARGUMENTS

The ask text is what the functional review judges against. Record the item in the project tracker (Jira if available, else plandb / BACKLOG.md) and reference it in the PR body.
